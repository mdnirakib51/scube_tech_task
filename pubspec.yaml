name: scube_technologies_task
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  get: ^4.7.2 # State Management!
  intl: ^0.20.2 # Localization and Date Formatting
  image_picker: ^1.1.2 # UI Enhancements
  google_fonts: ^6.2.1 # Font Style!

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/app_icons/
    - assets/app_images/
    - assets/app_src/
    - assets/required_image/

